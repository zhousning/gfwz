#header-image.container.pt-3.pb-2
  .row
    .col-sm-4
      = image_tag "gslogo.jpg", :class => "w-100"
    .col-sm-2.offset-6
      = image_tag "lsswgzh.jpg"
%nav.navbar.navbar-expand-lg.navbar-dark.qijianbox-title.p-0
  .collapse.navbar-collapse.position-relative
    .container.px-0
      %ul#home-header-navbar.navbar-nav.w-100.justify-content-start.px-3
        %li.nav-item.py-2.show-on-hover
          %a.nav-link.text-white{:href => root_path}
            首页
        - Frst.order('sequence').each do |frst|
          %li.nav-item.py-2.show-on-hover
            %a.nav-link.text-white{:href => "javascript:void(0)"}
              = frst.name
            .container-fluid.dropdown-menu.garanty
              .container
                .row.rtl.text-center
                  - frst.secds.order('sequence DESC').each do |secd|
                    .col.box
                      - if secd.index_page
                        = link_to list_secd_articles_path(idencode(secd.id)), :class => "hvr-underline-from-left text-decoration-none" do 
                          = image_tag secd.header_url
                          %h5.text-dark
                            = secd.name
                      - elsif secd.articles.blank? 
                        = link_to "javascript:void(0)", :class => "hvr-underline-from-left text-decoration-none"  do 
                          = image_tag secd.header_url
                          %h5.text-dark
                            = secd.name
                      - else
                        = link_to info_secd_article_path(idencode(secd.id), idencode(secd.articles.first.id)), :class => "hvr-underline-from-left text-decoration-none"  do 
                          = image_tag secd.header_url
                          %h5.text-dark
                            = secd.name
          -#
            = link_to  frst.name,nil, :class => "nav-link text-white", :id => "navbarScrollingDropdown" + frst.id.to_s, :role => "button", :'data-toggle' => "dropdown", :'aria-expanded' => "false"
              
            %ul{:class => "dropdown-menu rounded-0", :'aria-labelledby' => "navbarScrollingDropdown" + frst.id.to_s}
              - frst.secds.each do |secd|
                %li
                  = link_to nil, secd.name, :class => 'dropdown-item'
                
              

      -#
        %li.nav-item.active
          = link_to labs_path, :class => "nav-link  " do
            = Setting.labs.tag
        %li.nav-item.active
          = link_to cstr_reports_path, :class => "nav-link  " do
            = Setting.reports.tag
        %li.nav-item.active
          = link_to activities_path, :class => "nav-link  " do
            = Setting.activities.tag
        %li.nav-item.active
          = link_to notices_path, :class => "nav-link  " do
            = Setting.notices.tag
        %li.nav-item.active
          = link_to infos_path, :class => "nav-link  " do
            = Setting.infos.tag
        %li.nav-item.active
          = link_to manages_path, :class => "nav-link  " do
            = Setting.manages.tag
